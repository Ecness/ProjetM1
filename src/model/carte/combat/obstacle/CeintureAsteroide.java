package model.carte.combat.obstacle;

import model.parametre.EnumTailleMapCombat;

public class CeintureAsteroide extends Obstacle{

	public CeintureAsteroide(EnumTailleMapCombat taille) {
//		super();
//		
//		int x1 = (int) ((taille.getTaille()-(2*taille.getZoneSpawn()))*Math.random()+taille.getZoneSpawn());
//		int x2 = (int) ((taille.getTaille()-(2*taille.getZoneSpawn()))*Math.random()+taille.getZoneSpawn());
//		
//		int y = (int) ((200*Math.random()+300)/2);		
//		
//		this.addCoordonee(x1, y);
//		this.addCoordonee(x1, y);
//		this.addCoordonee(x2, y);
//		this.addCoordonee(x2, y);
	}
}
